services:
  - type: web
    name: gym-backend
    runtime: node
    plan: free
    region: frankfurt
    rootDir: backend
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: DB_HOST
        value: mysql-xxx.render.com  # ← à remplacer par l’URL de ta base Render
      - key: DB_USER
        value: admin
      - key: DB_PASSWORD
        value: ton_mot_de_passe
      - key: DB_NAME
        value: salle_sport_POWERGYMNEFTA
      - key: JWT_SECRET
        value: cle_secrete_super
